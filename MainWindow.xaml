<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="505" Width="800">
    <Grid>
        <ListView x:Name="listview">
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="ID" Width="150" DisplayMemberBinding="{Binding ID}"/>
                    <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Surname" Width="150" DisplayMemberBinding="{Binding Surname}"/>
                    <GridViewColumn Header="PESEL" Width="150" DisplayMemberBinding="{Binding PESEL}"/>
                </GridView>
            </ListView.View>

        </ListView>
        <Button x:Name ="btnadd" Content="Button" HorizontalAlignment="Left" Margin="680,414,0,0" VerticalAlignment="Top" Height="38" Width="72" Click="btnadd_Click" />

    </Grid>

</Window>
